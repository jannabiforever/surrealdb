/**
[bench]
reason = "GraphQL list query selecting two fields from 1000 records"
run = true
rebuild = false
datasets = { "10k" = "bench/util/graphql-items-10000.surql" }
warmup = "2s"
sample-size = 50
measurement-time = "20s"

[test]
run = false
*/
query {
	items(limit: 1000) {
		number
		label
	}
}
