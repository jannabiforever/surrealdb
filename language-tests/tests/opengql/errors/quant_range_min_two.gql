/**
[test]
reason = "Lowering rejection: range quantifiers with a minimum above one are rejected for the same reason as fixed ones — the collect BFS drops nodes first discovered below the minimum depth."

[test.results]
parsing-error = """
Variable-length quantifiers with a minimum greater than one are not supported yet
  --> [15:28]
   |
15 | MATCH (a:person)-[:knows]->{2,3}(b:person) RETURN a
   |                            ^^^^^ only `{1}` and `{1,n}` quantifiers are supported
"""
*/

MATCH (a:person)-[:knows]->{2,3}(b:person) RETURN a
