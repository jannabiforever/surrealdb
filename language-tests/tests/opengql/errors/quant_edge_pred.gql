/**
[test]
reason = "Lowering rejection: the recursion lowering has no per-edge scope to evaluate an edge predicate in."

[test.results]
parsing-error = '''
Variable-length edge patterns cannot have a WHERE clause or property map
  --> [15:17]
   |
15 | MATCH (a:person)-[:knows {since: 2020}]->{1,2}(b:person) RETURN a
   |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ remove the predicate or the quantifier
'''
*/

MATCH (a:person)-[:knows {since: 2020}]->{1,2}(b:person) RETURN a
